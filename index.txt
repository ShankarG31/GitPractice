This is a "Hello World!" line.

git config --global user.name "username" 
git config --global user.email "email"
git init : Initialize an empty repository 
git add . : To add modified file to staging area
git reset : It is used to remove files from the staging area
git status : This show the state of working directory and staging area
git commit : This command is used to save the files that are currently in staging area.
git log : It is used to obtain the commit that have happened so far with their respective commit messages (detailed)
git log --oneline: It is used to obtain the commit that have happened so far with their respective commit messages (only commit id and message)
git push : It is used to upload local repository content to a remote repository(github). 
working on master branch 
